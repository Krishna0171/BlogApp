export const NotExists = (entity) => {
  return entity + " does not exist!";
};

export const AlreadyExist = (entity) => {
  return entity + " already exists!";
};

export const InvalidInput = (entity) => {
  return entity + " input is invalid!";
};

export const InvalidCredentials = "Invalid Credentials!";